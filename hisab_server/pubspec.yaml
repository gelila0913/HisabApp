name: hisab_server
description: A server app using the shelf package and Docker.
version: 1.0.0

environment:
  sdk: ^3.11.4

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.2
  mysql1: ^0.20.0  # Added this for XAMPP MySQL connection
  crypto: ^3.0.3   # Recommended for future password hashing

dev_dependencies:
  http: ^1.2.2
  lints: ^6.0.0
  test: ^1.25.6